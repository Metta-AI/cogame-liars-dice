version     = "0.1.0"
author      = "daveey"
description = "Liar's Dice: a bluffing coworld with cheap talk and a soft-play audit, for the Softmax Coworld platform. Four cogs, hidden hands, one escalating claim at a time; a policy is just a prompt."
license     = "MIT"

srcDir = "src"

requires "nim >= 2.2.4"
requires "bitworld >= 0.1.0"
requires "mummy >= 0.4.7"
requires "curly >= 1.1.1"
requires "whisky"
